# =====================================================================
#  Test configuration.
#  Any key can be overridden without editing this file:
#    1) JVM system property:  ./mvnw test -Dapp.port=9090
#    2) environment variable: APP_PORT=9090 ./mvnw test
#       (key upper-cased, dots -> underscores)
#  Priority: system property > environment variable > this file.
# =====================================================================

# --- Application under test ---
app.jar.path=app/internal-0.0.1-SNAPSHOT.jar
app.host=localhost
# 0 = pick a random free port (no clashes with anything already running)
app.port=0
app.endpoint.path=/endpoint
app.api.key=qazWSXedc
app.startup.timeout.seconds=60

# --- External service mock (WireMock) ---
# 0 = pick a random free port
mock.port=0
